#include <iostream>
#include <fstream>
#include <vector>
using namespace std;

ifstream cin("input.txt");
ofstream cout("output.txt");

int main() {
    int x, y, a, b, c, d, t;

    cin >> x >> y;
    cin >> a >> b;
    cin >> c >> d;

    while (!cin.eof()) {

    }






    return 0;
}
